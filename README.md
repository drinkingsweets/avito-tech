# Avito Tech - Pull Request Review System

Микросервис для управления Pull Request'ами и назначением ревьюверов в командах разработки.

## Содержание

[Описание проекта](#описание-проекта)
- [Технологии](#технологии)
- [Быстрый старт](#быстрый-старт)
- [Переменные окружения](#переменные-окружения)
- [Зависимости](#зависимости)
- [API Endpoints](#api-endpoints)
- [Аутентификация и авторизация](#аутентификация-и-авторизация)
- [Структура проекта](#структура-проекта)
- [Проблемы и решения](#проблемы-и-решения)
- [Тестирование](#тестирование)
- [Нагрузочное тестирование](#нагрузочное-тестирование)

---

## Описание проекта

Сервис предоставляет REST API для управления:
- **Командами разработчиков** (создание команд, управление участниками)
- **Пользователями** (активация/деактивация, просмотр назначенных PR)
- **Pull Request'ами** (создание, слияние, переназначение ревьюверов)

Система поддерживает ролевой доступ (ADMIN/USER) и валидацию данных в соответствии с OpenAPI спецификацией.

---

## Технологии

- **Java 17**
- **Spring Boot 3.5.7**
- **Spring Security 6.5.6** (JWT аутентификация)
- **Spring Data JPA** (работа с БД)
- **PostgreSQL 17.5** (база данных)
- **Hibernate 6.6.33** (ORM)
- **Lombok** (упрощение кода)
- **JJWT 0.11.5** (генерация и валидация JWT токенов)
- **JUnit 5 + Mockito** (тестирование)
- **Docker & Docker Compose** (контейнеризация)
- **Gradle 8.x** (сборка проекта)

---

## Быстрый старт

### Требования

- **Docker** 20.10+
- **Docker Compose** 1.29+
- **Java 17** (для локальной разработки, опционально)
- **Gradle 8.x** (для локальной сборки, опционально)

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/avito-tech.git
cd avito-tech
```

### 2. Создание .env файла

```bash
cp .env.example .env
```

**Содержимое .env:**
```
DB_URL=db
DB_PORT=5432
DB_NAME=avitotech
DB_USERNAME=postgres
DB_PASSWORD=postgres
POSTGRES_DB=avitotech
APP_PORT=8080
JWT_SECRET=ZWVkOGVlMzhlYTkxMzA3YTA5NDk4NDc0NzllYjczMmJhYWJiY2NkZGVlZmYwMDExMjIzMzQ0NTU2Njc3ODg5OWFhYmJjY2RkZWVmZjAwMTEyMjMzNDQ1NTY2Nzc4ODk5
JWT_EXPIRATION=86400000
```

### 3. Сборка приложения (локально)

```bash
./gradlew clean build -x test
```

Это создаст JAR файл в `build/libs/`.

### 4. Запуск через Docker Compose

```bash
docker-compose up -d
```

Сервис будет доступен на `http://localhost:8080`
База данных PostgreSQL на порту `5432`, с предзаполненными двумя пользователями u_admin, u_user, u1, u2 и командой backend чтобы генерировать токен авторизации

### 5. Проверка работоспособности

```bash
curl http://localhost:8080/health
```

Ожидаемый ответ:
```json
{
  "status": "UP"
}
```

### 6. Остановка сервиса

```bash
docker-compose down
```

### 7. Полная очистка (с удалением данных БД)

```bash
docker-compose down -v
```

---

## Переменные окружения

Проект использует следующие переменные окружения для конфигурации:

### `.env` файл

Создайте файл `.env` в корне проекта:

```bash
# Database Configuration
DB_URL=localhost
DB_PORT=5432
DB_NAME=avitotech
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Application Configuration
APP_PORT=8080

# JWT Configuration
JWT_SECRET=<jwt secret, 512b>
JWT_EXPIRATION=86400000
```

### Описание переменных

| Переменная | Описание | Пример | Требования |
|-----------|---------|--------|-----------|
| `DB_URL` | Хост PostgreSQL | `localhost` или `db` (Docker) | IP адрес или hostname |
| `DB_PORT` | Порт PostgreSQL | `5432` | Обычно 5432 для PostgreSQL |
| `DB_NAME` | Имя базы данных | `avitotech` | Буквы и цифры |
| `DB_USERNAME` | Пользователь БД | `postgres` | Обычно `postgres` |
| `DB_PASSWORD` | Пароль БД | `postgres` | Min 8 символов, сильный пароль в production |
| `POSTGRES_DB` | Название базы в контейнере | `avitotech` | Название в одно слово, без пробелов |
| `APP_PORT` | Порт приложения | `8080` | Свободный порт (1024-65535) |
| `JWT_SECRET` | Секретный ключ JWT (Base64) | Закодированная строка 64+ байта | **Min 64 байта для HS512!** |
| `JWT_EXPIRATION` | Срок действия токена (мс) | `86400000` | 24 часа в мс |

### Как генерировать JWT_SECRET

Для HS512 алгоритма необходимо минимум **64 байта/512 бит** в Base64 формате:

**Linux/Mac:**
```bash
openssl rand -base64 64
```

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String((1..64 | ForEach-Object {Get-Random -Maximum 256}))
```

**Java:**
```java
import java.util.Base64;
String secret = Base64.getEncoder().encodeToString(new SecureRandom().generateSeed(64));
System.out.println(secret);
```

### Как использовать переменные

**Через Docker Compose:**
```bash
docker-compose --env-file .env up -d
```

**Локально (IntelliJ IDEA):**
- Run -> Edit Configurations
- Environment variables -> скопируйте переменные из `.env`

**Локально (Gradle):**
```bash
./gradlew bootRun \
  -Dspring.datasource.url=jdbc:postgresql://localhost:5432/avitotech \
  -Dspring.datasource.username=postgres \
  -Dspring.datasource.password=postgres
```

---

## Зависимости

Все зависимости объявлены в `build.gradle.kts`:

### Spring Boot Framework

```kotlin
// JPA для работы с базой данных
implementation("org.springframework.boot:spring-boot-starter-data-jpa")

// Spring Security для аутентификации и авторизации  
implementation("org.springframework.boot:spring-boot-starter-security")

// Валидация данных (JSR-303)
implementation("org.springframework.boot:spring-boot-starter-validation")

// REST API endpoints
implementation("org.springframework.boot:spring-boot-starter-web")
```

**Зачем:**
- **JPA** — ORM для взаимодействия с PostgreSQL через Hibernate
- **Security** — управление ролями, фильтры, защита эндпоинтов через JWT
- **Validation** — @Valid, @NotBlank, @NotEmpty аннотации для валидации DTO
- **Web** — Spring MVC для REST контроллеров

### JWT (JSON Web Tokens)

```kotlin
implementation("io.jsonwebtoken:jjwt-api:0.11.5")
runtimeOnly("io.jsonwebtoken:jjwt-impl:0.11.5")
runtimeOnly("io.jsonwebtoken:jjwt-jackson:0.11.5")
```

**Версия:** 0.11.5  
**Зачем:**
- Генерация JWT токенов с подписью HS512
- Валидация подписи и срока действия
- Извлечение claims (userId, role, iat, exp)
- Использует Jackson для JSON сериализации

### XML Binding (Jakarta)

```kotlin
implementation("jakarta.xml.bind:jakarta.xml.bind-api:4.0.4")
```

**Зачем:**
- Требуется для Java 17+ (XML обработка)
- Необходимо для работы с некоторыми Spring компонентами

### Database Driver

```kotlin
runtimeOnly("org.postgresql:postgresql")
```

**Зачем:**
- PostgreSQL JDBC драйвер для подключения к БД
- Необходим для работы Hibernate

### Lombok

```kotlin
compileOnly("org.projectlombok:lombok")
annotationProcessor("org.projectlombok:lombok")
```

**Зачем:**
- `@Data` — автогенерация getter, setter, equals, hashCode, toString
- `@Builder` — паттерн Builder для создания объектов
- `@Slf4j` — автоматический логгер
- Упрощает и ускоряет разработку

### Testing Dependencies

```kotlin
testImplementation("org.springframework.boot:spring-boot-starter-test")
testImplementation("org.springframework.security:spring-security-test")
testRuntimeOnly("org.junit.platform:junit-platform-launcher")
```

**Зачем:**
- `spring-boot-starter-test` — MockMvc, @SpringBootTest, Mockito, AssertJ
- `spring-security-test` — @WithMockUser, SecurityMockMvcRequestPostProcessors.csrf()
- `junit-platform-launcher` — запуск JUnit 5 тестов

### Версионирование

| Зависимость | Версия | Причина |
|------------|--------|---------|
| Spring Boot | 3.5.7 | LTS, стабильная, Java 17 поддержка |
| Spring Security | 6.5.6 | Совместима с Boot 3.5.7 |
| Hibernate | 6.6.33 | JPA 3.1, лучшая производительность |
| JJWT | 0.11.5 | Стабильная, HS512 поддержка |
| Lombok | Latest | Встроенная в Boot поддержка |
| PostgreSQL Driver | Latest | Свежие версии баз данных |
| JUnit 5 | Встроена | Современный стандарт Java тестирования |

---

## API Endpoints

### Аутентификация

| Метод | Endpoint | Описание | Роль |
|-------|----------|----------|------|
| POST | `/auth/admin-token` | Получить JWT токен с ролью ADMIN | Нет |
| POST | `/auth/user-token` | Получить JWT токен с ролью USER | Нет |

### Управление командами

| Метод | Endpoint | Описание | Роль |
|-------|----------|----------|------|
| POST | `/team/add` | Создать команду с участниками | ADMIN |
| GET | `/team/get?team_name=...` | Получить информацию о команде | ADMIN/USER |

### Управление пользователями

| Метод | Endpoint | Описание | Роль |
|-------|----------|----------|------|
| POST | `/users/setIsActive` | Изменить статус активности пользователя | ADMIN |
| GET | `/users/getReview?user_id=...` | Получить PR на ревью пользователя | ADMIN/USER |

### Управление Pull Request'ами

| Метод | Endpoint | Описание | Роль | Ошибки |
|-------|----------|----------|------|--------|
| POST | `/pullRequest/create` | Создать PR с ревьюверами | ADMIN | ALREADY_EXISTS (400), NOT_FOUND (404) |
| POST | `/pullRequest/merge` | Слить PR (изменить статус на MERGED) | ADMIN | NOT_FOUND (404), INVALID_STATE (422) |
| POST | `/pullRequest/reassign` | Переназначить ревьювера | ADMIN | ALREADY_EXISTS (400), NOT_FOUND (404), NO_CANDIDATE (409) |

---

## Аутентификация и авторизация

### Схема работы JWT

Проект использует **JWT (JSON Web Token)** для аутентификации и авторизации пользователей.

#### Как это работает

**1. Получение токена(пользователи u_admin или u_user):**
```bash
POST /auth/admin-token
Body: {"userId": "u_admin"}

Response:
{
  "token": "eyJhbGciOiJIUzUxMiJ9...",
  "role": "ADMIN",
  "userId": "u_admin"
}
```

**2. Использование токена:**
Все защищённые эндпоинты требуют заголовок:
```
Authorization: Bearer eyJhbGciOiJIUzUxMiJ9...
```

**3. Валидация токена:**
- `JwtAuthenticationFilter` перехватывает запросы
- Извлекает токен из заголовка `Authorization`
- Валидирует подпись и срок действия
- Устанавливает аутентификацию в `SecurityContext`

**4. Проверка роли:**
Spring Security автоматически проверяет роль через `@PreAuthorize("hasRole('ADMIN')")`

#### Структура JWT токена

```json
Header: {
  "alg": "HS512",
  "typ": "JWT"
}

Payload: {
  "sub": "u_admin",         // userId
  "role": "ADMIN",          // роль пользователя
  "iat": 1700000000,        // время создания
  "exp": 1700086400         // время истечения (24 часа)
}

Signature: HMAC-SHA512(secret)
```

#### Безопасность

- Секретный ключ хранится в переменной окружения `JWT_SECRET` (минимум 64 байта для HS512)
- Токен действителен 24 часа (`JWT_EXPIRATION=86400000`)
- Алгоритм подписи: **HS512** (HMAC with SHA-512)
- Проверка сроков действия и подписи на каждый запрос
- Ролевой контроль доступа (ADMIN/USER)

---

## Структура проекта

```
avito-tech/
├── src/
│   ├── main/
│   │   ├── java/org/example/avitotech/
│   │   │   ├── controller/       # REST контроллеры (Team, User, PullRequest)
│   │   │   ├── dto/              # DTO для запросов/ответов
│   │   │   ├── exception/        # Обработка ошибок (ApiException, ErrorCode)
│   │   │   ├── jwt/              # JWT компоненты (JwtTokenProvider, JwtAuthenticationFilter)
│   │   │   ├── model/            # JPA Entity (Team, User, PullRequest, AssignedReviewer)
│   │   │   ├── repository/       # Spring Data JPA репозитории
│   │   │   ├── service/          # Бизнес-логика (TeamService, UserService, PullRequestService)
│   │   │   └── config/           # Конфигурация Spring Security
│   │   └── resources/
│   │       └── application.properties
│   └── test/
│       └── java/org/example/avitotech/ # Интеграционные тесты    
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Dockerfile для приложения
├── build.gradle.kts              # Gradle сборка
├── Makefile                      # Команды для управления проектом
├── README.md                     # Этот файл
└── .gitignore                    # Git исключения
```

---

## Проблемы и решения

### 1. Проблема: JJWT 0.11.5 — `UnknownClassException`

**Описание:**  
При запуске возникала ошибка:
```
Unable to load class named [io.jsonwebtoken.impl.DefaultJwtBuilder]
```

**Причина:**  
Отсутствовала runtime зависимость `jjwt-impl` и `jjwt-jackson`.

**Решение:**
```kotlin
dependencies {
    implementation("io.jsonwebtoken:jjwt-api:0.11.5")
    runtimeOnly("io.jsonwebtoken:jjwt-impl:0.11.5")
    runtimeOnly("io.jsonwebtoken:jjwt-jackson:0.11.5")
}
```

---

## Тестирование

### Unit & Integration тесты

Проект покрыт интеграционными тестами для всех контроллеров:

```bash
# Запустить все тесты
./gradlew test

# Запустить тесты конкретного класса
./gradlew test --tests TeamControllerTest
./gradlew test --tests UserControllerTest
./gradlew test --tests PullRequestControllerTest

# С отчётом покрытия
./gradlew test jacocoTestReport
```

Отчёт о покрытии: `build/reports/jacoco/test/html/index.html`

---

## Нагрузочное тестирование

### Параметры тестирования

Проведено нагрузочное тестирование с использованием Apache JMeter:

| Параметр | Значение |
|----------|----------|
| Потоки (одновременные пользователи) | 50 |
| Повторы на пользователя | 10 |
| Время запуска (ramp-up) | 10 секунд |
| Всего запросов | 1500 (50 × 10 × 3 эндпоинта) |
| Эндпоинты для тестирования | `/health`, `/users/getReview`, `/team/get` |
| Время тестирования | ~30 секунд |

### Результаты нагрузочного тестирования

#### Summary Report

| Метрика | Результат | Статус    |
|---------|-----------|-----------|
| Всего запросов | 1500 | OK        |
| Успешных запросов | 1500 (100%) | OK        |
| Неудачных запросов | 0 (0%) | OK        |
| **Среднее время отклика** | **2 ms** | OK        |
| Минимальное время ответа | 0 ms | OK        |
| Максимальное время ответа | 26 ms | OK        |
| Стандартное отклонение | 2.07 ms | OK        |
| **Пропускная способность** | **152.4 req/sec** |  Высокая |

#### Результаты по эндпоинтам

**GET /health:**
- Запросов: 500
- Среднее время: 1 ms
- Ошибок: 0 (0%)
- Пропускная способность: 50.8 req/sec

**GET /users/getReview:**
- Запросов: 500
- Среднее время: 3 ms
- Ошибок: 0 (0%)
- Пропускная способность: 50.9 req/sec

**GET /team/get:**
- Запросов: 500
- Среднее время: 2 ms
- Ошибок: 0 (0%)
- Пропускная способность: 51.0 req/sec

#### 📊 Оценка готовности к production

| Критерий | Оценка | Статус |
|----------|--------|-------|
| Стабильность | 10/10 | Готов |
| Производительность | 9/10 | Готов |
| Обработка ошибок | 10/10 | Готов |
| Масштабируемость | 8/10 | Нужна оптимизация |
| **Общая оценка** | **9.25/10** | **ГОТОВ К PRODUCTION** |

Система готова к развертыванию в production с текущей конфигурацией для обслуживания небольших-средних нагрузок.


---

## Makefile

### Доступные команды

```bash
# Собрать проект
make build

# Запустить Docker Compose
make up

# Остановить Docker Compose
make down

# Просмотр логов
make logs

# Перезапустить сервис
make restart

# Очистка (удалить volumes)
make clean

# Запустить тесты
make test

# Здоровье приложения
make health

# Shell в контейнере приложения
make shell-app

# psql в контейнере БД
make shell-db
```

---

## .gitignore

Убедитесь, что файл `.env` находится в `.gitignore`:

```
# Environment files
.env
.env.local
.env.*.local

# Build files
build/
.gradle/
out/

# IDE
.idea/
.vscode/
*.iml

# System
.DS_Store
Thumbs.db

# Logs
*.log
```